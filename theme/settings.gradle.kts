// Standalone build for the Livewire IDE theme plugin.
// Kept out of the root KMP build intentionally — the IntelliJ Platform Gradle
// plugin brings its own repositories and IDE dependencies that don't belong
// in the library build.
//
// Build from this directory: ../gradlew buildPlugin
rootProject.name = "livewire-theme"
